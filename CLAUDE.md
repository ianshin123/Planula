# Planula 저장소 작업 지침

이 문서는 GitHub에 연결된 코딩 세션이 이 저장소를 수정할 때 따르는 기준이다. 특정 컴퓨터의 지속적인 로컬 세션, 설치된 `gh`, 이미 실행 중인 서버를 전제하지 않는다.

## 저장소와 실행 구조

- 단일 파일 PWA다. 앱의 HTML·CSS·JavaScript는 `index.html`에 인라인으로 있고 별도 빌드 체인은 없다.
- 정적 파일은 `manifest.webmanifest`, `sw.js`, `icon-192.png`, `icon-512.png`, `.nojekyll`이다.
- GitHub 저장소가 코드의 정본이다. 변경은 현재 작업 브랜치에서 하고, 기본 브랜치에 직접 덮어쓰지 않는다.
- GitHub Pages는 저장소에 설정된 배포 브랜치를 따른다. 작업 브랜치 push만으로 운영 페이지에 반영된다고 단정하지 않는다.
- `index.html`·`manifest.webmanifest`·`sw.js`가 참조하는 manifest·아이콘·마스코트 URL에는 캐시 버전 쿼리를 붙인다. 쿼리 없이 두면 파일 내용을 바꿔도 파일명이 같아 브라우저와 서비스워커가 옛 파일을 계속 준다. `sw.js`의 `CACHE`를 올릴 때 세 파일의 쿼리 번호를 모두 같은 값으로 맞춘다.
- 앱 데이터는 브라우저 `localStorage`에만 있다. 서버, 계정, 토큰, 동기화, 외부 API를 추가하지 않는다.

## 언어

- 화면 문구는 한국어와 영어 두 벌을 유지하되, 지금 켜 둔 언어는 `LANG_ON`이 정한다. 현재는 `['ko']`라 어느 기기에서도 한국어로 나온다. 영어 문구를 다시 다듬은 뒤 `'en'`을 넣으면 기기 언어(`navigator.languages[0]`) 감지가 그대로 살아난다. 영어 사전과 조판 규칙(`:root[data-lang="en"]`)은 지우지 않는다. 사용자가 앱 안에서 고르는 언어 설정은 두지 않는다.
- 문구는 `L` 한 객체에 모아 둔다. 새 문구를 넣을 때는 반드시 두 언어를 함께 넣는다. 화면에 보이는 글자를 코드에 직접 적지 않는다.
- 언어는 저장 데이터에 남기지 않는다. 저장 키 `planner_v3`, 필드 이름(`subjects`, `subj` 등), 내보내기 형식은 언어와 무관하게 그대로다. 한국어로 만든 데이터를 영어 기기에서 열어도 사용자가 적은 이름은 적은 그대로 보인다.
- 날짜는 `fmtMD`·`fmtMDW`·`fmtMDWShort`·`fmtDot`·`fmtYM`이 언어별로 만든다. 한국어는 숫자 중심(`8월 15일 토요일`), 영어는 약어 월 이름(`Sat, Aug 15`)이다. 길이는 `durTxt`가 `1시간 30분` / `1h 30m`으로 낸다.
- 튜토리얼 단계는 순서·조작 대상만 `TOUR_STEPS`에 두고 대사는 `L.tour[id]`에 둔다. 단계를 더할 때 두 언어의 대사를 함께 채운다.
- 정적 마크업(독 라벨, FAB)의 문구는 `data-i18n`·`data-i18n-aria`로 표시하고 부팅할 때 갈아 끼운다.
- 영어는 낱말이 길어 한국어용 자간·줄바꿈을 그대로 쓰면 성기게 읽힌다. `:root[data-lang="en"]`에서 자간을 되돌리고 낱말 단위 줄바꿈으로 바꾼다. 헤더는 제목·마스코트·아이콘 네 개가 한 줄에 들어가지 않으므로 영어에서만 제목·아이콘·마스코트를 한 단계씩 줄인다.
- 탭 이름은 짧게 유지한다. 헤더 제목(`.h1 .hname`)은 한 줄 말줄임이라 길어도 마스코트를 덮지 않지만, 잘린 제목은 그 자체로 실패다.

## 데이터 모델

저장 키는 `planner_v3`이고 런타임 객체는 `DB`다.

```text
DB.tasks[]    {id,title,date,min,dur,autoSlot,ord,done,subj,repId}
DB.subjects[] {id,name,color,ddays:[{id,title,date}],goals:[{id,text}]}
DB.repeats[]  {id,title,subj,start,end,freq,weekdays,interval,min,dur,mode24,ex}
DB.s          {force24:{'YYYY-MM-DD':boolean},theme:'sys'|'light'|'dark',
               scheduleProfiles:[{id,name,off,blocks:[{id,name,s,e}]}],
               dayProfiles:{'0'..'6':profileId},defaultSchedOff:boolean,
               defaultBlocks:{weekday:[{id,name,s,e}],weekend:[...]},
               tutorialSeen:boolean,introDone:boolean}
```

### 데이터 불변조건

1. `task.min`은 자정 기준 절대 분(0~1435)이며 `null`이면 미배정이다. 블록 id(`p1`, `ja1` 등)를 task에 저장하지 않는다.
2. `task.dur`는 생성 시 확정한다. 보기 모드가 바뀌어도 저장된 시각과 길이는 바뀌지 않는다.
3. 저장 키와 `{min,dur}` 구조를 임의로 바꾸지 않는다. `load()`의 옛 데이터 보정과 `planner_v3_bak_grid` 백업을 유지하고, 데이터 시트의 되돌리기 경로(`restoreBackup()`)를 살려 둔다.
4. `task.autoSlot`은 주말 24시간 보기에서 기본 60분 일정이 공유하는 상대 시간 슬롯 시작값이다. 수동으로 길이를 조절하면 해당 슬롯 일정들의 `autoSlot`을 지워 자동 재분할 대상에서 제외한다.
5. 사용자 시간표는 `scheduleProfiles[]`에 이름과 시간대 목록으로 저장하고 `dayProfiles`가 요일을 시간표에 연결한다. 한 시간표는 여러 요일에 적용할 수 있고, 어떤 요일에도 연결하지 않은 채 보관할 수도 있다.
6. 시간대에는 **기본 시간대**가 있다. 평일(`DEFAULT_WEEKDAY` — 1교시~야자 2차시)과 주말(`DEFAULT_WEEKEND` — 오전·점심·오후·저녁·밤) 두 벌이고, 코드에 있는 상수라 `scheduleProfiles[]`에 저장하지 않는다. 큰 덩어리로만 나누면 칸이 쪼개지는 감각이 와닿지 않아 실제 고등학생 하루를 그대로 쓴다. `scheduleProfile(ds)`의 순서는 **그 요일에 연결된 켜진 사용자 시간표 → 켜져 있는 기본 시간대(그 날이 주말이면 주말 쪽) → 없음(24시간 보기)**이다.
7. 시간대는 지우지 않고 켜고 끈다. 사용자 시간표는 `off` 필드, 기본 시간대는 `DB.s.defaultSchedOff`로 관리한다. 첫 사용자 시간표를 저장하면 기본 시간대가 저절로 꺼지고, 사용자 시간표를 모두 지우면 다시 켜진다. 둘 다 꺼져 있으면 그 요일은 24시간 보기다.
8. 기본 시간대는 **고쳐 쓰는 출발점**이다. 평일·주말 카드의 `수정`으로 칸을 더하거나 지우고 시각을 바꾼 결과는 **기본 시간대의 고친 버전**(`DB.s.defaultBlocks.weekday/weekend`)으로 저장한다. 내 시간표(`scheduleProfiles[]`)가 되는 것이 아니다. 내 시간표는 `새 시간표`로 따로 만든다. 고친 버전이 없으면 코드에 있는 원래 값을 쓰고, `원래 기본값으로 되돌리기`로 언제든 지운다. 기본을 고쳐도 `defaultSchedOff`는 켜진 채다.
9. 기본 시간대 편집기에서는 이름과 적용 요일을 보여주지 않는다. 평일은 월~금, 주말은 토·일로 정해져 있고 이름도 정해져 있다. 칸만 고친다.
10. 반복은 `DB.repeats[]`의 규칙과 `ex[]` 예외목록으로 관리한다. 인스턴스를 대량 선생성하지 않는다.
11. `materialize()`는 반복 규칙의 절대 `min`과 `dur`를 그대로 복사한다. 학교 보기와 24시간 보기 사이에서 슬롯 이름으로 재배치하지 않는다.

`LEGACY_CLASSES`와 `legacyProfile()`은 실제 할 일·목표·반복 데이터가 있는 v4 이하 사용자 데이터를 처음 v5로 옮길 때만 쓰는 기본 학교 시간표다. 내용 없는 구버전 저장값과 과거 마이그레이션이 자동 생성한 미사용 `default-school` 시간표는 빈 설정으로 정리한다. 새 사용자는 자기 시간표 없이 기본 시간대로 시작한다. 기존 데이터 마이그레이션이나 ‘학교 예시 불러오기’ 외의 런타임 배경은 사용자가 저장한 시간표 아니면 기본 시간대를 쓴다.

12. 런타임 데이터 버전은 v6다. v5 이하 파일이나 기존 `planner_v3` 저장값은 `normalizeDB()`가 할 일·목표·반복을 보존한다. `defaultSchedOff` 값이 없는 저장값은 이미 만든 시간표가 있으면 꺼짐, 없으면 켜짐으로 채운다. 실제 플래너 데이터가 있는 구버전에만 평일용 레거시 시간표를 더하고, 내용 없는 구버전은 빈 24시간 보기로 시작한다. 저장 키는 기존 설치 호환을 위해 그대로 유지한다.
13. 내보내기에는 `DB` 전체와 시간표 설정이 포함된다. 가져오기 직전 현재 상태를 `planner_v3_bak_import`에 저장하고, 사용자가 데이터 화면에서 즉시 복원할 수 있게 한다.

## 일정 생성과 편집

- 새 일정 시트는 이름과 목표만 받으며 항상 미배정으로 생성한다. 시작·종료 시각 증감 UI를 다시 넣지 않는다.
- 시트를 열 때 입력창에 자동으로 포커스를 주지 않는다. 사용자가 입력창을 직접 눌러야 키보드가 올라온다. 할 일·목표·하위 목표 시트 모두 같다.
- 추가는 오른쪽 아래 큰 FAB 하나로 통일한다. 같은 버튼이 탭 문맥에 맞게 오늘은 새 미배정 일정, 목표 탭은 새 목표, 달력 탭은 오늘 날짜 팝업을 연다. 헤더에 별도의 추가 버튼을 두지 않고, 빈 화면 안내와 튜토리얼도 이 FAB을 가리킨다.
- 빈 그리드 탭은 선택 해제만 한다. 빈 곳 탭으로 일정을 만들지 않는다.
- 배정 일정과 미배정 칩은 모두 첫 탭에서 선택만 한다. 선택된 항목을 다시 탭하면 편집하고, 선택된 상태에서만 이동하거나 배정할 수 있다.
- 배정 일정의 크기 조절은 선택했을 때 보이는 위·아래 주황 손잡이를 잡은 경우에만 시작한다. 보이는 막대는 58×9px이지만 잡히는 범위는 `::before`로 상하 13px·좌우 26px 넓힌다. 막대 크기 그대로면 손가락이 자꾸 빗나간다. 그 밖의 카드 본문은 선택된 상태에서 끌었을 때 일정 이동으로 처리한다.
- 탭과 끌기의 경계는 `TAP_SLOP`(12px) 하나로 둔다. 이보다 작으면 선택된 일정을 다시 눌러 수정하려 할 때 손가락 흔들림이 끌기로 새어 나가 수정 시트가 열리지 않는다.
- 선택되지 않은 배정·미배정 항목의 드래그는 이동으로 시작하지 않는다. 미배정 칩에도 배정 일정과 같은 선택 테두리를 표시한다. 칩 안의 완료 버튼(`.uck`)은 선택과 무관하게 미배정 상태를 유지한 채 완료만 전환한다.
- 선택되지 않은 배정 일정 위에서 시작한 세로 드래그는 카드가 가로채지 않고 화면 스크롤로 동작해야 한다. `.ev`는 기본적으로 `touch-action: pan-y`를 사용하고, 선택되거나 실제 이동 중인 카드만 `touch-action: none`으로 전환한다.
- 할일 수정 시 `미배정으로 복사`를 제공한다. 복사본은 같은 날짜의 미배정 목록에 제목과 목표만 복사하고, 새 id·미완료·반복 없음 상태의 독립 일정으로 만든다.
- 배정 일정의 완료 버튼(`.evck`)과 미배정 완료 버튼(`.uck`)은 선택·드래그 제스처와 분리한다.
- 전날의 미완료 일정은 오늘 탭 최상단의 정리 카드에 표시한다. 각 항목은 전날 기록에서 완료, 삭제, 오늘 미배정으로 이동 중 하나를 바로 선택할 수 있다.
- 반복 일정의 전날 인스턴스를 삭제하거나 오늘로 옮기면 원래 날짜를 반복 규칙의 `ex[]`에 추가해 다시 생성되지 않게 한다. 오늘로 옮긴 사본은 반복 규칙에서 분리하고 `min·dur = null`인 미배정 일정으로 만든다.
- 롱프레스에 기능을 배정하지 않는다.

## 타임그리드와 드래그

- 하루의 시작은 02:00(`DAY0`)이다. `rel(min)`으로 화면 좌표의 분을 구하고 `abs(rel)`로 절대 분을 복원한다. 02:00 전의 `TODAY()`는 전날이다.
- 기본 시간축은 `PPM = 1.4` px/분이다. 20px보다 짧은 카드는 같은 시간 칸 안의 빈 공간으로 최소 터치 높이를 먼저 확보한다. 다음 일정뿐 아니라 시간표 밴드 끝(24시간 보기에서는 다음 정각)까지 남은 공간이 20px보다 작을 때만 `timeMap()`이 해당 분 구간을 부분 확대해 5·10분 카드가 칸 밖으로 튀어나오지 않게 한다.
- 길이 조절, 자동 분할, 직접 끌어 배치하는 좌표 모두 5분 단위다. 배치만 10분으로 두면 6시 정각처럼 5분 끝자리가 필요한 시각을 맞출 수 없다.
- 시간선, 사용자 시간표 배경 밴드, 일정, 현재 시각, 스크롤 이동, 터치 좌표는 반드시 같은 `timeMap()`을 사용한다. 저장된 `min·dur` 자체는 바꾸지 않는다.
- `bindGrid()`가 쓰는 `map`은 제스처가 시작될 때(`touchstart`)마다 다시 계산한다. 바인딩 시점 값을 클로저에 가둬 두면 짧은 일정이 생긴 뒤 그린 좌표와 만지는 좌표가 갈라진다. 한 번의 드래그 도중에는 다시 그리지 않으므로 그 제스처 안에서는 같은 `map`을 유지한다.
- `bindGrid()`는 한 노드에 한 번만 붙인다. `_b` 표시를 되돌려 다시 호출하면 같은 노드에 리스너가 겹쳐 완료 토글이 짝수 번 실행된다. 날짜 팝업을 갱신할 때는 `#popbody`의 내용만 다시 그리고 다시 바인딩하지 않는다.
- 짧은 일정은 보이는 카드와 별도로 주변 22px의 가상 터치 영역을 갖고, 그리드 오른쪽 44px는 가상 완료 영역이다.
- 점심·저녁 같은 구간은 블록으로 만들지 않고 칸 사이를 비워 둔다. `gapName()`이 시간대 범위로 이름을 붙이고, 빈 구간은 평일 쉬는시간과 같은 회색으로 그려진다. 블록으로 만들면 혼자 주황색으로 떠 어긋나 보인다.
- 시간표 보기에서는 사용자가 만든 시간대와 그 사이의 빈 시간을 모두 드롭 대상으로 사용한다. 120분을 넘는 긴 빈 구간은 `bandAt()`이 손가락 위치의 1시간 슬롯으로 나눠 새벽 일정이 거대한 블록으로 변하지 않게 한다.
- 주말 기본 24시간 보기에서는 아직 수동으로 길이를 바꾸지 않은 기본 60분 일정에 `autoSlot`을 둔다. 같은 60분 슬롯에 다른 기본 일정을 놓으면 평일 블록처럼 슬롯 안을 균등 분할하고, 하나가 빠지면 남은 일정이 다시 채운다. 길이를 수동 조절한 일정이나 다른 길이의 일정은 이 자동 분할에 합류시키지 않는다.
- 같은 블록에 여러 일정이 있으면 `splitBand()`가 드롭 순서를 보존하고 블록을 균등 분할한다. 일정이 빠지면 남은 일정이 블록을 다시 채운다.
- 배정 일정을 움직일 때는 실제 요소를 `paintEl()`로 움직인다. 미배정 칩을 배정할 때만 `.dropghost`로 최종 위치와 크기를 미리 보여준다.
- 아래 손잡이가 바로 다음 일정과 맞닿으면 경계를 함께 이동해 총 길이를 보존한다.
- 위 손잡이가 바로 앞 일정과 맞닿은 경우에도 같은 경계를 함께 이동한다. 한쪽을 늘리면 이웃 일정의 시작 또는 끝이 따라오며 둘 사이에 빈틈이나 겹침이 생기지 않는다.
- 겹침은 레인으로 나누지 않고 `resolveOverlaps()`로 경계를 정리한다.
- 시트는 `overflow-x: hidden`이다. 손잡이의 넓힌 `::before`가 시트 폭을 넘기 때문에, 이걸 빼먹으면 시트 안이 가로로 스크롤돼 날짜 팝업 내용이 좌우로 밀린다.
- 오른쪽 sticky 레일은 미배정 목록이자 되돌리기 드롭 대상이다. 그 위에서는 자동 스크롤을 멈춘다.
- 미배정 레일은 스크롤 위치와 관계없이 위아래가 잘리지 않아야 한다. 높이를 화면 크기만으로 계산하면 맨 아래로 내렸을 때 sticky 가 풀리며 위가 잘리고 아래는 독에 가린다. `fitRail()`이 **실제로 보이는 세로**와 **`.daylayout`이 차지할 수 있는 세로(`#scroll` 높이 − 아래 패딩)** 중 작은 쪽을 재서 `--railh`에 넣는다. 뒤엣것을 빼먹으면 맨 아래에서 다시 잘린다.
- 자동 스크롤은 실제 스크롤 영역의 위·아래 가장자리만 기준으로 한다. 미배정 레일의 위치를 스크롤 경계 계산에 섞지 않는다.
- 화면 왼쪽에 스크롤용 반원 다이얼(`#rail`)을 두지 않는다. 시간 이동은 그리드 스크롤로만 한다. 오늘 탭을 처음 열거나 다른 탭에서 돌아오거나 이미 열린 오늘 탭을 다시 누르면 현재 시각선이 화면 위쪽 약 1/3 지점에 오도록 맞춘다.

## 화면 구조와 디자인

- 색상은 따뜻한 크림 배경과 Claude 계열 주황을 사용한다. 라이트 `--ac`는 `#E07A4F`, 다크는 `#FF9264`다.
- 색상 선택 스와치는 36px 원이다. 한 벌 전체가 시트에서 두 줄 안에 들어오게 유지한다.
- 목표 색 `PALETTE`는 색상환을 고르게 도는 10색 한 벌이다. 밝기와 채도를 맞춰 라이트·다크 어디서나 같은 계열로 읽히게 하고, 첫 색은 강조색과 같은 테라코타로 둔다. 저장된 색은 값 그대로 남으므로 팔레트를 바꿔도 기존 목표의 색은 변하지 않는다.
- `.glass`는 단순 반투명 표면에 일반 `blur()`만 사용한다. 채도 증폭, brightness, 다중 내부광, 복잡한 유리 그라디언트는 넣지 않는다.
- 완료 카드는 전체 `opacity`를 낮추지 않는다. `--done-bg` 반투명 배경과 `blur(8px)`로 뒤를 부드럽게 비치고, 텍스트만 약하게 흐린다. 작은 일정에서도 흰 체크 표시가 보여야 한다.
- 깊이는 얇은 경계와 `--shadow-sm/md/lg` 세 단계로만 표현한다. 큰 그림자는 시트·드래그·FAB처럼 실제로 떠 있는 요소에만 쓴다.
- 공통 반경은 `--r`, `--r2`, `--r3`를 우선 사용한다. 같은 위계의 버튼과 카드가 제각각인 반경을 갖지 않게 한다.
- 태블릿 콘텐츠 최대폭은 `--wrap:780px`, 미배정 레일은 `--unw:144px`다. 620px 이하에서는 시간 거터와 미배정 레일을 줄인다.
- `#top`과 `#scroll`의 좌우 여백은 어느 화면폭에서도 같은 값을 유지한다. 한쪽만 바꾸면 헤더와 본문이 어긋나고 모든 탭의 내용이 한쪽으로 치우친다.
- 하단 독은 떠 있는 블러 표면을 사용한다. FAB는 독 옆에 두고 헤더로 옮기지 않는다.
- 세 번째 탭의 이름은 `목표`/`Goals`다. 색·디데이·하위 목표를 묶는 단위이고 할 일이 여기에 연결된다. 저장 필드 이름은 호환을 위해 `subjects`·`subj`로 둔다. 화면 이름과 저장 필드 이름이 다르다는 사실을 지우지 않는다.
- 독 탭은 라벨 길이에 따라 폭이 늘어난다(`min-width` + 좌우 여백). 고정 폭으로 되돌리면 긴 영어 라벨이 알약 밖으로 삐져나온다.
- 시트와 날짜 팝업은 바깥 빈 곳을 눌러 닫는 것 외에 맨 위 손잡이를 잡아 아래로 끌어내려도 닫힌다. 시트 자체가 스크롤 영역이므로 스크롤과 헷갈리지 않게 조건을 좁게 잡는다. 손잡이(`.grab`, `::before`로 시트 폭 전체·위아래 13px 넓힌 영역)에서 시작한 터치만 후보로 보고, 가로로 더 움직이거나 위로 올리면 즉시 손을 떼 스크롤에 맡기며, 아래로 8px을 넘겨야 끌어내리기로 확정한다. 손잡이는 시트 맨 위에 있으므로 스크롤을 내린 상태에서는 애초에 잡히지 않는다.
- 오늘 탭은 오늘만 보여준다. 다른 날짜는 달력 셀의 날짜 팝업으로 연다.
- 전날 미완료 정리 카드가 있으면 오늘 탭은 현재 시각으로 내리지 않고 화면 맨 위에서 시작한다. 모두 정리됐을 때만 현재 시각 포커스를 사용한다.
- 2주 이내 마감은 헤더 아래의 작은 빨간 텍스트로만 표시한다.
- 테마는 `DB.s.theme = sys|light|dark`와 `data-theme`으로 적용한다.
- 일정의 목표는 왼쪽 색선이 아니라 제목 옆의 색점과 이름으로 표시한다. 이름은 `<b>`로 감싸 말줄임한다. flex 컨테이너의 맨 텍스트에는 `text-overflow`가 걸리지 않아 이름이 말줄임 없이 뚝 잘린다.
- 앱 본문에 로고를 넣지 않는다. 홈 화면 아이콘만 사용한다.
- 홈 화면 아이콘은 마스코트를 확대해 얼굴과 다리 윗부분만 담고 배경은 라이트모드 배경색 `#F7F5F1`을 쓴다. 머리 사각형이 maskable 안전영역(중심 반지름 40%) 안에 들어가야 하며, 다리는 아래 모서리로 흘려보낸다. `icon-192.png`와 `icon-512.png`는 같은 구도로 만든다.
- 문서 제목과 설치 이름은 영문 `Planula`로 통일한다. 저장 키 `planner_v3`와 `assets/planner-mascot-*` 자산 경로는 기존 설치 호환을 위해 이름을 바꾸지 않는다.
- 설치형 앱처럼 쓰므로 핀치·더블탭·트랙패드 확대를 허용하지 않는다. viewport의 `maximum-scale=1, user-scalable=no`, 루트 `touch-action: pan-x pan-y`, iOS `gesture*` 차단을 함께 유지하되 일반 세로 스크롤과 탭 간 가로 스와이프는 보존한다.
- 사용법 중에는 `새 시간표`를 만들지 않는다. 새 사용자는 기본 시간대 아니면 기본의 고친 버전으로 시작한다. 시간대 단계는 저장으로 넘어가지 않는다 — 평일 하나를 고치고 저장해도 그 자리에 남아 주말도 고칠 수 있어야 한다. 넘어가는 길은 버튼뿐이다.
- 목록에서 카드 본문을 누르면 고르기만 하고, 편집기는 오른쪽 `수정` 버튼으로만 연다. 이름 부분을 눌렀다고 편집기가 열리면 눌러 본 사람이 당황한다.
- 사용법은 **9단계**다. 말을 늘리지 말고, 시작할 때 반드시 알아야 하는 것만 남긴다. 순서는 시간대 → 목표 → 디데이 → 할 일 두 개 → 수정 → 배치 → 분할 → 비율 조절 → 테마·다시 보기다. 여기에 없던 단계(설치 안내, 달력, 날짜 팝업, 미배정 설명, 데이터 백업)는 되살리지 않는다.
- **건너뛸 수 있는 단계는 시간대 하나뿐이다.** 나머지는 사용자가 실제로 해내야 넘어간다(`signal` + `need`). 그래서 말풍선에 끝내기·이전 버튼을 두지 않는다. 통째로 건너뛰는 길은 시작 질문의 `써 봤어요` 하나다.
- 한 번의 저장으로 부족한 단계는 `TOUR_NEED`가 조건을 본다. 할 일은 두 개(`twoUnassigned`), 분할은 같은 칸에 둘(`shareBand`)이어야 넘어간다. 조건을 못 채우면 그 단계에 그대로 머문다.
- 단계는 `id`로 식별하고 대사는 `L.tour[id]`에 둔다. 번호로 가리키면 단계를 하나 끼워 넣을 때 모든 분기가 조용히 어긋난다.
- 그리드 강조 칸은 **일정이 실제로 들어가는 칸**이다. 시간표 보기에서는 `bandAt()`이 주는 밴드 통째로 들어가므로 한 시간으로 그리면 안내와 결과가 어긋난다. 24시간 보기에서만 다음 정각까지의 한 시간을 쓴다. 끌어 놓기 유령 카드의 도착 크기도 강조 칸 그대로다.
- 강조 상자만으로는 무엇을 하라는 건지 전달되지 않는다. `hintKind`로 동작을 보여준다. `press`는 대상 자체를 살짝 키웠다 되돌리고(160px 이하 버튼·칩만 — 큰 카드를 키우면 화면이 출렁인다), `drag`는 미배정에서 시간 칸으로 옮기는 동작을, `grab`은 아래 손잡이를 잡아 늘리는 동작을 흐릿한 유령 카드로 반복 재생한다.
- 유령 카드(`#tourdemo`)는 스크롤할 때마다 다시 만들지 않는다. `_tourHintKey`가 같으면 그대로 둔다. 매번 새로 만들면 애니메이션이 계속 처음으로 튄다.
- 시트가 열려 있는 동안에는 말풍선과 캐릭터가 시트를 덮지 않는다. 판단 기준은 장면 이름이 아니라 `tourSheetEl()` — 지금 시트가 열려 있는가다. 장면으로 판단하면 다른 단계에서 시트가 떠도 자리를 비우지 않는다.
- 시트는 올라오면서 움직인다. 그 동안 강조 박스가 옛 자리에 남지 않게 `startTourTracking()`은 항상 돈다. 특정 장면에서만 돌리면 시트가 뜨는 다른 단계에서 강조 박스가 어긋난 자리에 찍힌다.
- 시트가 뜨는 단계의 `selector`에는 시트 안의 입력창을 앞에, 시트 밖의 버튼을 뒤에 적는다. 그래야 시트가 열리는 순간 강조가 새 입력창으로 옮겨가고, 시트 뒤에 가린 옛 대상에 남지 않는다.
- 캐릭터는 강조 칸도 말풍선 글씨도 덮지 않는다. 자리 점수는 강조 칸 겹침 > 말풍선 겹침 > 화면 밖 순으로 벌점을 준다. 강조 칸이 커서 말풍선 옆자리가 전부 막히면 **강조 칸의 위·아래**를 후보에 더한다. 말풍선에서 좀 떨어지더라도 글씨를 덮는 것보다 낫다.
- 강조 테두리의 발광은 `--ac-glow`·`--spot-glow`로 테마마다 따로 둔다. 라이트 강조색을 다크에 그대로 쓰면 색이 어긋나 지저분하게 번진다.
- 안내 문구는 `tourType()`으로 한 글자씩 적어 나간다. 높이는 완성본으로 먼저 재서 고정하므로 타자 도중 말풍선이 흔들리지 않는다. 말풍선을 누르면 즉시 완성한다.
- 처음 여는 사람에게는 튜토리얼을 바로 띄우지 않고 `openIntro()`의 시작 질문을 먼저 보여준다. `처음이에요`는 튜토리얼을 그대로 시작하고, `써 봤어요`는 통째로 건너뛰며 캐릭터만 헤더로 날려보낸다. 답한 사실은 `DB.s.introDone`에 남기지만 재등장 판단에는 쓰지 않는다.
- 시작 질문의 재등장 기준은 오직 **만든 데이터가 하나도 없는 상태**다. 할 일·목표·반복·시간표·요일별 보기 변경이 없고 기본 테마인 초기 상태라면, 전에 답한 적이 있고 `tutorialSeen`이 서 있어도 새로고침할 때마다 다시 연다. 사용자가 만든 데이터가 하나라도 생기면 자동 재등장을 멈춘다. 다시 보기는 헤더 캐릭터의 말풍선에서만 연다.
- 튜토리얼이 켜져 있는 동안에는 지금 안내하는 곳만 만질 수 있다. `tourGateAllows()`가 허용하는 것은 `#tour`의 버튼, `#scrim`·`#subscrim`, 현재 단계의 강조 대상, 단계가 `allow`로 더 지정한 곳, 그리고 열린 시트다. 나머지는 창 캡처 단계에서 막는다. 터치 기본동작은 막지 않아 세로 스크롤은 그대로 된다.
- 잠금은 그 단계를 끝까지 마칠 수 있어야 한다. 필수 단계라 더 그렇다. 사용자의 조작으로 새로 열린 시트는 저장 버튼까지 열어 주고, 미배정에서 집어 시간 칸에 놓는 것처럼 두 곳을 오가는 단계는 `allow`에 양쪽을 모두 적는다. 안내해 놓고 막아 두면 사용자가 갇힌다.
- 날짜 팝업의 FAB는 `#sheet` 밖의 `#popfab`에 둔다. transform이 있는 시트 안의 `position:fixed`로 옮기지 않는다.
- 날짜 팝업에서 할일을 추가·수정할 때는 날짜 시트를 덮어쓰지 않고 `#subsheet`를 한 층 더 띄운다. 바깥을 누르거나 작업을 끝내면 기존 날짜와 스크롤 위치로 돌아가며, 날짜 시트의 빈 공간 탭은 오늘 탭처럼 선택만 해제한다.
- 날짜 팝업을 열거나 날짜를 넘기면 `#sheet.scrollTop = 0`으로 시작해 02:00부터 보여준다. 07:00으로 자동 스크롤하지 않는다.
- 달력은 `fitCalendar()`로 한 화면에 맞춘다.
- 이미 열린 달력 탭을 다시 누르면 `calM`을 현재 달로 되돌리고 달력 상단에 맞춘다. 이때 토스트를 띄우지 않는다. 화면이 이미 이번 달을 보여주므로 안내가 겹친다.

## GitHub 연동 작업 방식

1. GitHub 연결 또는 현재 checkout에서 저장소·브랜치·변경 상태를 먼저 확인한다. 다른 저장소나 사용자의 관련 없는 변경을 읽거나 수정하지 않는다.
2. 변경 요청은 현재 작업 브랜치에서 구현한다. 작업 트리가 섞여 있으면 관련 파일만 명시적으로 stage한다.
3. `index.html`을 수정하면 인라인 `<script>`를 추출해 `node --check`로 문법 검사한다. 실행 도구가 없으면 가능한 정적 검사를 하고 제한을 명시한다.
4. UI나 제스처 변경은 사용 가능한 브라우저에서 실제 렌더와 콘솔 오류를 확인한다. 로컬 서버나 브라우저 접근이 제공되지 않으면 우회하지 말고, 좌표·상태 전이를 단위 검사한 뒤 검증 한계를 보고한다.
5. `index.html` 또는 정적 배포 파일을 바꾸면 `sw.js`의 현재 `CACHE` 값을 확인하고 한 단계 올린다.
6. 구현과 검증이 끝나면 관련 파일만 구체적인 한국어 메시지로 커밋하고 작업 브랜치에 push한다. GitHub 연동이 쓰기를 지원하지 않으면 커밋 또는 패치를 남기고 막힌 지점을 설명한다.
7. 변경 브랜치를 push한 뒤 GitHub 연동으로 PR을 만들고 검사가 통과하면 기본적으로 squash merge한다. 필수 검사가 대기 중이면 auto-merge를 켠다. 충돌이나 실패가 있을 때만 멈춰 원인을 해결한다.
8. PR 생성·리뷰·CI·머지는 GitHub 연동 기능을 우선 사용한다. 사용자가 직접 PowerShell이나 `gh`를 실행해야 한다고 전제하지 않는다.

## 커뮤니케이션

- 소유자는 신이안이다.
- 결과, 실제 원인, 검증 범위, 남은 위험을 구체적으로 말한다. 막연한 칭찬이나 완료를 가장한 표현을 쓰지 않는다.
