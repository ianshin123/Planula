# CLAUDE.md — 플래너 앱 (Plan)

> 이 repo를 다루는 **Claude Code** 세션의 단일 행동 지침.
> ⚠️ 볼트(`Brain`) 규칙과 섞지 마라. 거기는 회고 이력 금지, 여기는 그냥 앱 코드다.

## 프로젝트
- **단일 파일 PWA**: 전부 `index.html`(HTML+CSS+JS 인라인). 빌드 체인 없음.
- **배포**: GitHub Pages(`.nojekyll`). `index.html` push → 바로 반영. `sw.js`는 HTML network-first라 즉시 최신, 정적자산 cache-first.
- **탭 3개**: 오늘(today) · 달력(cal) · 과목(subj). 하단 도크 + 좌우 스와이프 전환.
- **완전 로컬**. 서버·토큰·동기화·외부 요청이 **하나도 없다.** 학종 볼트 연동은 폐지했다 — 다시 넣지 마라.

## 데이터 모델 (localStorage `planner_v3` = `DB`)
```
DB.tasks[]    {id,title,date,min,dur,ord,done,subj,rep,repId}
DB.subjects[] {id,name,color,ddays:[{id,title,date}],goals:[{id,text}]}
DB.s.force24  {'YYYY-MM-DD': true|false}   // 그 날짜의 24시간 모드 강제
```

### 🔒 불변조건 1 — 배치의 정본은 `min`(절대 분)이다
`task.min` = 자정 기준 분(0~1435), **null이면 미배정**. 렌더는 `min`이 어느 블록 구간에 들어가는지로 결정한다.
**블록 id(`p1`, `ja1` …)를 task에 저장하지 마라.** 그러면 24시간 모드로 바꿨을 때 그 블록이 없어져 **할일이 화면에서 통째로 증발한다** — 실제로 그 버그를 내고 고쳤다.

### 🔒 불변조건 2 — 시간표는 자동으로 24시간을 덮는다
`CLASSES`에 수업 블록만 적으면 `schoolBlocks()`가 **사이 빈 구간을 쉬는시간/점심/저녁으로 자동 생성**해 00:00~24:00에 빈틈이 없게 만든다. 수업 시간이 바뀌면 `CLASSES`만 고쳐라.

**북일고 시간표** (분 단위): 점호 410–435 · 아침조례 490–500 · 1교시 510–560 · 2교시 570–620 · 3교시 630–680 · 4교시 690–740 · 5교시 810–860 · 6교시 870–920 · 7교시 930–980 · 8교시 1010–1080 · 야자1 1150–1260 · 야자2 1275–1410.

### 🔒 불변조건 3 — 반복은 시각을 그대로 복사한다
`materialize()`는 `min`을 **그대로** 복사한다. 평일 야자1차시(19:10) 할일을 매일 반복하면 **토·일(24시간 모드)에도 19:10**에 놓여야 한다. 슬롯 이름으로 옮기려 들지 마라.

### 🔒 불변조건 4 — 반복은 '규칙 + 예외목록'이다
`DB.repeats[]`에 규칙(`freq`·`weekdays`·`interval`·`start`·`end`·`min`·`dur`·`ex[]`)을 두고, `materialize(날짜)`가 그 날짜의 인스턴스를 만든다. 인스턴스를 미리 120개 찍어두는 방식으로 되돌리지 마라 — 시작·종료일 변경과 개별 삭제를 못 한다.
- **개별 삭제** = 그 날짜를 `rule.ex`에 넣고 인스턴스 제거(재생성돼도 안 살아난다).
- **전체 삭제** = 규칙과 인스턴스 모두 제거.

### 🔒 불변조건 5 — 새 할일은 이름(+과목)만 받는다
**새 할일 시트에는 시간대·소요·반복을 넣지 마라.** 이름과 과목만 받고 저장한다. 자세한 편집은 **만든 뒤 꾹 눌러 수정**에서 한다(`taskSheet`가 `_ed.id` 유무로 갈린다).
- **칸을 아무 데나 탭하면** 그 시각으로 추가된다(`blockTap`). 칸 안의 작은 `+` 버튼은 없앴다.
- **큰 FAB은 미배정**으로 넣는다.
- 칩 탭은 수정(`chipTap`), 체크박스는 완료 토글. 드래그 직후 420ms는 탭을 무시한다.

할일은 **이름 + 언제**만으로 저장된다. `dur`가 `null`이면 **그 칸 길이만큼**으로 친다(`taskDur`). 5분 단위 조정은 '세부 설정'에 접어두고 기본으로 펼치지 마라.

### 🔒 불변조건 6 — '언제'는 보기 모드에 따라 다르다
- **학교 시간표 모드**: 칸을 탭해 고른다. **이어서 다른 칸을 탭하면 걸쳐진다**(`tapBlock` — 범위 밖이면 늘리고, 범위 안이면 그 칸 하나로 줄인다). 걸친 할일은 시작 칸에 칩이 뜨고 나머지 칸엔 `.cont`(이어짐) 표시가 붙는다.
- **24시간 모드**: 칸 목록 대신 **시작·종료 시각을 30분 단위**로 고른다(`bumpStart`/`bumpEnd`). 주말뿐 아니라 **24시간으로 바꾼 평일도** 마찬가지다 — 기준은 요일이 아니라 `blocksFor(date).is24`.
- 24시간 모드에서 만든 반복은 규칙에 `mode24:true`가 붙고, **그 루틴이 평일에 오면 그 날짜도 자동으로 24시간 보기로 바뀐다**(`materialize`). 안 그러면 09:00~11:00 루틴이 학교 시간표의 엉뚱한 칸에 처박힌다.

### 🔒 불변조건 7 — 디자인 언어
- **색은 Claude 계열 주황** (`--ac` 라이트 `#E07A4F` / 다크 `#FF9264`), 배경은 따뜻한 크림.
- **리퀴드 글래스**: 카드·독·시트는 `.glass`(= `--glass-bg/brd/shadow/blur`)를 쓴다. 평평한 단색 카드로 바꾸지 마라.
- **독은 떠 있다**: 화면 아래 중앙 고정, 반투명, 좌우로 **썸을 끌어 탭 전환**(`attachDockDrag`). 할일 추가는 **독 옆 FAB**이지 헤더가 아니다.
- **오늘 탭은 오늘만 보여준다.** 날짜 이동 버튼을 두지 마라. 다른 날은 **달력에서 날짜를 눌러 팝업**(`openDay`)으로 연다 — 팝업 안에서도 드래그·추가가 된다.
- **다가오는 디데이는 오늘 탭 맨 위 가로 레일**(`railHTML`)에 뜬다.
- **테마는 사용자가 고른다**: `DB.s.theme` = `sys|light|dark`, 헤더 버튼으로 순환. `data-theme` 속성으로 적용한다.
- **광은 아껴 쓴다.** 글로우는 **기능 강조에만** — 디데이 색 바, 현재 시각 블록, 드래그 타깃. 체크박스·칩·버튼·달력 셀에 광을 두르지 마라(한 번 전부 발라봤다가 "유리느낌 너무 강하다"고 지적받았다).
- **앱 안에 로고를 넣지 않는다.** 홈화면 아이콘(`icon-192/512.png`)은 주황 마름모다.
- 아이패드를 많이 쓴다 → `#view`는 `--wrap`(720px)로 폭을 제한하고, 타임테이블 블록은 **세로로 넉넉히**(gap 64px / 수업 74px 이상).
- 달력은 `fitCalendar()`로 **한 화면에 들어오게** 높이를 계산한다.
- 날짜 팝업의 **큰 + 는 `#popfab`으로 시트 바깥(body)에 둔다.** `#sheet`에 `transform`이 걸려 있어서 그 안에 넣으면 `position:fixed`가 뷰포트가 아니라 시트 기준이 되고, 버튼이 화면 밖으로 밀린다 — 실제로 그랬다. `sticky`도 안 먹는다.
- 과목 색 선택은 **원형 스와치**(`.swatches .sw`).

## 상호작용 규칙
- **꾹 누르기(250ms) → 들어올림 → 드래그 → 드롭.** 안 움직이고 떼면 **편집 시트**가 열린다. 이 둘은 같은 제스처에서 갈린다.
- 드래그 중엔 **타깃 블록 강조 + 드롭 위치 라인 + 시각 안내 배지**가 보여야 한다.
- 한 블록에 여러 할일 → 그 안에서 순서 변경.
- 정밀 조정은 **5분 단위**. 블록 경계를 넘기면 자연스럽게 옆 블록으로 재배치된다.
- 과목의 **하위 목표는 체크박스도 디데이도 아니다.** 추가·삭제·순서변경만.

## 작업 규칙
1. `index.html` 편집 후 **반드시 JS 문법 검사**: 인라인 스크립트 추출 → `node --check`. (한글 多, 템플릿 문자열 주의.)
2. **문법 통과 ≠ 동작.** 반드시 실제로 띄워서 확인:
   `python -m http.server 8099` → 브라우저에서 세 탭 렌더 + 콘솔 예외 0 + **드래그를 터치 이벤트로 시뮬레이션**해 검증.
3. 배포 파일(`index.html`·`sw.js`) 변경 시 **`sw.js`의 `CACHE` 버전 bump.** 현재 값은 `sw.js`에서 직접 확인할 것 — 여기 숫자를 적어두면 반드시 낡는다.
4. 커밋은 작은 단위, 메시지는 한글로 구체적으로. 커밋 후 **푸시까지.**

## 톤
소유자 = 신이안. 비판적·직설적, 막연한 칭찬 X, 실질 피드백.
